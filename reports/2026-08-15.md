# 科研雷达 · 2026-08-15

今日共聚合 2 条动态。

## 今日研究方向建议

1. **精读今日 Planet AI 文章 "Fine-Tuning Tool-Calling LLMs: A Complete Guide Using XYZ-Aquila-SFT and Qwen3" 的 ChatML 格式渲染与轨迹解析部分，将其作为 vLLM 中 agentic 场景下 prompt 结构化的直接参考。** 该文展示的工具调用轨迹解析与结构化提取流程，直接关联你关注的节约 tokens 多轮对话方案。建议你重点理解其"轨迹 → ChatML 模板"的映射规则，思考如何将多轮工具调用历史压缩为更紧凑的模板格式。然后在本地 vLLM 中，使用 Qwen2.5-7B 模拟一个 10 轮 API 调用任务，对比"原始 JSON 轨迹拼接"与"按该文 ChatML 模板渲染"在输入 token 数、KV Cache 占用和端到端延迟上的差异，重点关注结构化模板对多轮 agentic 场景中 token 成本的实际削减效果。

2. **分析今日 Simon Willison 文章 "Don't classify. Hallucinate!" 的"生成-匹配"两阶段标签方案，将其作为 vLLM 中 speculative decoding 草稿生成策略的类比参考。** 该文先让模型自由生成再与标签库匹配，直接关联你关注的投机采样优化。建议你阅读其向量嵌入匹配部分，理解"自由生成 + 检索约束"如何平衡探索与准确性。然后在本地 vLLM 中，使用 LLaMA-3-8B 作为草稿、LLaMA-3-70B 作为目标，对比"固定 top-k 采样草稿"与"按该文思路的生成-匹配草稿（先自由生成 10 个候选，再与高频 token 库匹配筛选）"在接受率、验证开销和端到端延迟上的差异，重点关注该策略对高熵 token 分布场景下投机解码鲁棒性的实际提升。

3. **结合今日 Planet AI 文章 "Fine-Tuning Tool-Calling LLMs" 中的 LoRA 适配流程，将其作为 Blackwell 上 FP8 推理 BERT 时精度补偿的轻量化参考。** 该文展示的 PyTorch LoRA 微调管线，直接关联你关注的 FP8 推理与 BERT 优化。建议你阅读其 LoRA 超参设置与适配层设计部分，思考：若在 FP8 量化 BERT 的 attention 输出层插入低秩适配器，能否补偿低精度带来的精度损失？可在本地 TensorRT-LLM 中，使用 BERT-base 在 Blackwell 上对比"直接 FP8 量化"与"FP8 量化 + LoRA 适配层（rank=8）"在 GLUE 基准准确率、显存占用和吞吐上的差异，重点关注该策略对低精度 BERT 推理中精度-性能权衡的实际效果。

4. **关注今日 Simon Willison 文章 "Don't classify. Hallucinate!" 的嵌入匹配思想，将其作为 vLLM 中 prefix-caching 跨模型复用的语义化参考。** 该文通过向量嵌入匹配而非精确字符串匹配来关联标签，直接关联你关注的 KV Cache 管理与节约 tokens 方向。建议你阅读其嵌入匹配的阈值设定部分，理解如何在语义等价但表面形式不同的输入间复用缓存。然后在本地 vLLM 中，使用 Qwen2.5-7B 测试"精确前缀匹配"与"按该文思路的嵌入相似度匹配（如余弦相似度 > 0.9 即复用）"在缓存命中率、TTFT 和显存占用上的差异，重点关注该策略对多轮对话中用户表述变体场景的实际收益。

## 研究者 / 博客（2）

### [Fine-Tuning Tool-Calling LLMs: A Complete Guide Using XYZ-Aquila-SFT and Qwen3](https://www.marktechpost.com/2026/08/15/fine-tuning-tool-calling-llms-a-complete-guide-using-xyz-aquila-sft-and-qwen3/)
_Planet AI · 2026-08-15 11:28 UTC_

**TL;DR**: 该教程展示了使用XYZ-Aquila-SFT和Qwen3对工具调用LLM进行端到端微调的完整流程，包括轨迹解析、结构化工具调用提取、ChatML格式渲染以及基于PyTorch的LoRA适配。

**Why it matters**: 为工具调用LLM的微调提供了实用指南，有助于提升模型在复杂任务中的工具使用能力，对推理系统优化有直接参考价值。

### [Don't classify. Hallucinate!](https://simonwillison.net/2026/Aug/14/dont-classify-hallucinate/)
_Simon Willison · 2026-08-14 21:54 UTC_

**TL;DR**: 提出了一种新的标签分类方法：先让模型自由生成标签，再通过向量嵌入与现有标签库匹配，以解决标签过多无法直接分类的问题。

**Why it matters**: 该方法利用LLM的生成能力和向量检索，为大规模标签分类提供了高效方案，与LLM推理和检索增强生成相关。
