# 科研雷达 · 2026-05-25

今日共聚合 25 条动态。

## 今日研究方向建议

1. **跟进 TensorRT-LLM #13773 中 FlashInfer NVFP4 MoE 后端的实现，评估其在 Blackwell (SM120/SM121) 上对 Nemotron 模型解码阶段的加速效果。** 该 PR 表明解码阶段比 CUTLASS 快约 17.6%，直接关联你的 FP4 量化、MoE dispatch 和新硬件优化方向。建议你阅读 `tensorrt_llm/plugins/flashinfer_moe_plugin.cpp` 中 NVFP4 MoE 内核的调用逻辑，重点关注 `sm120` 架构下的内核选择条件。然后，在本地 TensorRT-LLM 环境中，使用一个 Nemotron 模型的 NVFP4 量化版本，在 Blackwell GPU 上对比 FlashInfer 与 CUTLASS 后端的解码吞吐和显存带宽利用率，验证 17.6% 的加速是否可复现，并记录该后端对 MoE 层推理的优化细节。

2. **分析 SGLang #26295 中 HiCache 堆栈分发重构为策略模式的代码，评估其对扩展新 KV 池组合的灵活性和性能影响。** 该重构将 if/elif 链替换为策略模式，直接关联你的 KV Cache 管理和推理系统架构优化方向。建议你阅读 `sglang/sglang/layers/kv_cache/hicache.py` 中策略类的定义和 `dispatch` 函数的实现，重点关注策略选择逻辑如何与现有的 PagedAttention 和 prefix-caching 交互。然后，在本地 SGLang 环境中，使用一个支持 HiCache 的模型（如 DeepSeek-V4），对比重构前后在多种 KV 池组合（如 RadixCache + HiCache）下的解码吞吐和首 token 延迟，验证重构是否引入了性能退化或提升了扩展性。

3. **研究 SGLang #22851 中 `--dsa-topk-backend` 选项的实现，评估其对 RL 训练中 GLM-5 模型推理性能的影响。** 该 PR 集成了 FlashInfer 和 PyTorch 的 topk 实现，直接关联你的推理系统优化和 RL 训练支持方向。建议你阅读 `sglang/sglang/serve/engine.py` 中 `--dsa-topk-backend` 参数的注册逻辑和 `sglang/sglang/layers/topk.py` 中 FlashInfer topk 内核的调用实现。然后，在本地 SGLang 环境中，使用 GLM-5 模型，在 RL 训练场景下对比 FlashInfer 与 PyTorch topk 后端的解码吞吐和显存占用，记录 FlashInfer 后端在何种 batch size 和序列长度下更优，并评估其对 RL 训练吞吐的提升。

4. **跟进 HuggingFace Transformers #46199 中 XPU autocast dtype fallback 的支持，评估其对 FlashAttention 在 Intel XPU 上推理兼容性的影响。** 该 PR 为 FlashAttention 添加了 XPU 自动混合精度回退支持，直接关联你的推理系统兼容性优化方向。建议你阅读 `transformers/src/transformers/models/llama/modeling_llama.py` 中 FlashAttention 调用逻辑的变更，重点关注 `autocast` 回退的触发条件和 dtype 选择逻辑。然后，在本地 Transformers 环境中，使用一个 LLaMA 模型，在 Intel XPU 上对比启用/禁用该回退时的推理输出一致性和首 token 延迟，验证回退是否消除了 FlashAttention 在 XPU 上的精度问题。

5. **评估 vLLM/llm-compressor #2755 中 Qwen3.5 预量化检查点的文档，分析其对量化推理部署路径的参考价值。** 该 PR 列出了 10 个覆盖多种量化格式的检查点，直接关联你的量化推理和模型部署方向。建议你阅读该 PR 的文档变更，重点关注 `docs/source/models/qwen3.5.md` 中预量化检查点的列表和每种量化格式（如 W4A16、FP8）的适用场景。然后，在本地 vLLM 环境中

## GitHub 仓库动态（24）

### [[sgl-project/sglang] [FlashInfer v0.6.10] [RL] [DSv32] [GLM-5] Add `--dsa-topk-backend` and integrate FlashInfer and pytorch topk (#22851)](https://github.com/sgl-project/sglang/commit/2b9dd9c8b339bb397a3bbb56ff4bc330627d1033)
_sgl-project/sglang · commit · 2026-05-25 20:08 UTC_

**TL;DR**: 为 SGLang 添加 `--dsa-topk-backend` 选项，集成 FlashInfer 和 PyTorch 的 topk 实现，支持 RL 训练中的 GLM-5 模型。

**Why it matters**: 提升 RL 训练中 topk 操作的确定性和长上下文性能，优化大模型推理效率。

### [[sgl-project/sglang] #22851 [FlashInfer v0.6.10] [RL] [DSv32] [GLM-5] Add `--dsa-topk-backend` and integrate FlashInfer and pytorch topk](https://github.com/sgl-project/sglang/pull/22851)
_sgl-project/sglang · PR · 2026-05-25 20:08 UTC_

**TL;DR**: 与 idx 0 相同，为 SGLang 添加可配置的 topk 后端，集成 FlashInfer 和 PyTorch 实现。

**Why it matters**: 与 idx 0 相同，提升 RL 训练中 topk 操作的确定性和长上下文性能。

### [[huggingface/transformers] #46142 Fix TypeError on list-typed ignore_keys_at_rope_validation in RoPE config](https://github.com/huggingface/transformers/pull/46142)
_huggingface/transformers · PR · 2026-05-25 18:39 UTC_

**TL;DR**: 修复 HuggingFace Transformers 中 `ignore_keys_at_rope_validation` 类型错误，当从 JSON 加载为列表时与集合运算冲突。

**Why it matters**: 修复了合并后的 LoRA 检查点无法加载的问题，提升模型部署兼容性。

### [[NVIDIA/TensorRT-LLM] #13773 [None][feat] FlashInfer NVFP4 MoE backend (SM120/SM121) for Nemotron …](https://github.com/NVIDIA/TensorRT-LLM/pull/13773)
_NVIDIA/TensorRT-LLM · PR · 2026-05-25 16:25 UTC_

**TL;DR**: 为 TensorRT-LLM 添加 FlashInfer NVFP4 MoE 后端，支持 SM120/SM121 架构，在解码阶段比 CUTLASS 快约 17.6%。

**Why it matters**: 显著提升 MoE 模型在 NVIDIA 新硬件上的推理性能，优化解码阶段效率。

### [[NVIDIA/TensorRT-LLM] [None][feat] FlashInfer NVFP4 MoE backend (SM120/SM121) for Nemotron … (#13773)](https://github.com/NVIDIA/TensorRT-LLM/commit/63b1d8d9b2bddfa49047f00eab4945961184cc26)
_NVIDIA/TensorRT-LLM · commit · 2026-05-25 16:25 UTC_

**TL;DR**: 与 idx 4 相同，为 TensorRT-LLM 添加 FlashInfer NVFP4 MoE 后端。

**Why it matters**: 与 idx 4 相同，提升 MoE 模型推理性能。

### [[huggingface/transformers] #46199 Support XPU autocast dtype fallback for FlashAttention](https://github.com/huggingface/transformers/pull/46199)
_huggingface/transformers · PR · 2026-05-25 16:19 UTC_

**TL;DR**: 为 HuggingFace Transformers 的 FlashAttention 添加 XPU 自动混合精度回退支持。

**Why it matters**: 扩展 FlashAttention 对 XPU 硬件的支持，提升跨平台推理兼容性。

### [[sgl-project/sglang] #26295 Refactor HiCache stack dispatch into strategies](https://github.com/sgl-project/sglang/pull/26295)
_sgl-project/sglang · PR · 2026-05-25 16:06 UTC_

**TL;DR**: 重构 SGLang 的 HiCache 堆栈分发逻辑，使用策略模式替换 if/elif 链，便于扩展新的 KV 池组合。

**Why it matters**: 提升缓存系统的可扩展性和可维护性，有利于大模型推理中的 KV 缓存优化。

### [[sgl-project/sglang] Refactor HiCache stack dispatch into strategies (#26295)](https://github.com/sgl-project/sglang/commit/b13d3d18c68c5da56936b1b9aee9dcf467b65b91)
_sgl-project/sglang · commit · 2026-05-25 16:06 UTC_

**TL;DR**: 与 idx 7 相同，重构 HiCache 堆栈分发为策略模式。

**Why it matters**: 与 idx 7 相同，提升缓存系统可扩展性。

### [[huggingface/transformers] Support XPU autocast dtype fallback for FlashAttention (#46199)](https://github.com/huggingface/transformers/commit/7bc093b71ecc42204b48cd6abf65a437f73655ad)
_huggingface/transformers · commit · 2026-05-25 16:02 UTC_

**TL;DR**: 与 idx 6 相同，为 FlashAttention 添加 XPU 自动混合精度回退支持。

**Why it matters**: 与 idx 6 相同，扩展 FlashAttention 对 XPU 硬件的支持。

### [[sgl-project/sglang] [CI] Enable EPD CI for EPD architecture enhancements (#26281)](https://github.com/sgl-project/sglang/commit/2aa69953085de5963a9542ec7561b6f835402b8e)
_sgl-project/sglang · commit · 2026-05-25 15:52 UTC_

**TL;DR**: 为 EPD 架构增强启用持续集成测试，将测试从夜间运行改为基础 CI，以更早捕获回归。

**Why it matters**: 提升 LLM 推理系统 CI 可靠性，确保架构变更不破坏正确性。

### [[sgl-project/sglang] #26281 [CI] Enable EPD CI for EPD architecture enhancements](https://github.com/sgl-project/sglang/pull/26281)
_sgl-project/sglang · PR · 2026-05-25 15:52 UTC_

**TL;DR**: 为 EPD 架构增强启用持续集成测试，将测试从夜间运行改为基础 CI，以更早捕获回归。

**Why it matters**: 提升 LLM 推理系统 CI 可靠性，确保架构变更不破坏正确性。

### [[vllm-project/llm-compressor] #2755 [Docs] Add pre-quantized checkpoints to Qwen3.5 docs](https://github.com/vllm-project/llm-compressor/pull/2755)
_vllm-project/llm-compressor · PR · 2026-05-25 15:34 UTC_

**TL;DR**: 在 Qwen3.5 文档中添加预量化检查点章节，列出 10 个覆盖多种量化格式的检查点。

**Why it matters**: 方便用户直接使用预量化模型，减少推理部署中的量化开销。

### [[vllm-project/llm-compressor] [Docs] Add pre-quantized checkpoints to Qwen3.5 docs (#2755)](https://github.com/vllm-project/llm-compressor/commit/e69a250135f118ec75da50c36a261f20c4f14bdf)
_vllm-project/llm-compressor · commit · 2026-05-25 15:34 UTC_

**TL;DR**: 在 Qwen3.5 文档中添加预量化检查点章节，列出 10 个覆盖多种量化格式的检查点。

**Why it matters**: 方便用户直接使用预量化模型，减少推理部署中的量化开销。

### [[sgl-project/sglang] #25141 support kimi 2.5/6 lora (logprob diff exist)](https://github.com/sgl-project/sglang/pull/25141)
_sgl-project/sglang · PR · 2026-05-25 15:25 UTC_

**TL;DR**: 支持 Kimi 2.5/6 模型的 LoRA 微调，但存在 logprob 差异。

**Why it matters**: 扩展 SGLang 对最新模型的 LoRA 支持，但需注意精度问题。

### [[sgl-project/sglang] #25848 [diffusion] Add CFG gating for denoising](https://github.com/sgl-project/sglang/pull/25848)
_sgl-project/sglang · PR · 2026-05-25 14:57 UTC_

**TL;DR**: 为扩散模型去噪过程添加 CFG 门控，在指定步数后复用无条件分支的缓存差值，减少一次 transformer 前向计算。

**Why it matters**: 优化扩散模型推理速度，通过缓存减少计算量，提升生成效率。

### [[sgl-project/sglang] [diffusion] Add CFG gating for denoising (#25848)](https://github.com/sgl-project/sglang/commit/121cc094054f2fae39ca8c22e267c2edb63904d6)
_sgl-project/sglang · commit · 2026-05-25 14:57 UTC_

**TL;DR**: 为扩散模型去噪过程添加 CFG 门控，在指定步数后复用无条件分支的缓存差值，减少一次 transformer 前向计算。

**Why it matters**: 优化扩散模型推理速度，通过缓存减少计算量，提升生成效率。

### [[sgl-project/sglang] #25847 [diffusion] Cache fp32 layernorm params](https://github.com/sgl-project/sglang/pull/25847)
_sgl-project/sglang · PR · 2026-05-25 14:56 UTC_

**TL;DR**: 缓存 Wan 扩散路径中 FP32LayerNorm 的 fp32 参数副本，避免每次前向重复转换，提升推理性能。

**Why it matters**: 减少推理中的冗余计算，加速扩散模型推理。

### [[sgl-project/sglang] [diffusion] Cache fp32 layernorm params (#25847)](https://github.com/sgl-project/sglang/commit/85f9522e364bc543fa29c9b81cb006a476c98144)
_sgl-project/sglang · commit · 2026-05-25 14:56 UTC_

**TL;DR**: 缓存 Wan 扩散路径中 FP32LayerNorm 的 fp32 参数副本，避免每次前向重复转换，提升推理性能。

**Why it matters**: 减少推理中的冗余计算，加速扩散模型推理。

### [[huggingface/transformers] #46171 Enable passing kwargs through RoFormer models](https://github.com/huggingface/transformers/pull/46171)
_huggingface/transformers · PR · 2026-05-25 13:21 UTC_

**TL;DR**: 允许 RoFormer 模型传递 **kwargs 参数，以支持不同的注意力实现。

**Why it matters**: 增强 Transformers 库的灵活性，便于集成新的注意力机制。

### [[InternLM/lmdeploy] align streaming usage chunks with OpenAI spec (#4616)](https://github.com/InternLM/lmdeploy/commit/0cece4bc7387ab1bcf9076911cb0383c2b40d444)
_InternLM/lmdeploy · commit · 2026-05-25 12:47 UTC_

**TL;DR**: 修复了流式使用块与 OpenAI 规范的对齐问题。

**Why it matters**: 提升与 OpenAI API 的兼容性，简化用户迁移。

### [[InternLM/lmdeploy] #4616 align streaming usage chunks with OpenAI spec](https://github.com/InternLM/lmdeploy/pull/4616)
_InternLM/lmdeploy · PR · 2026-05-25 12:47 UTC_

**TL;DR**: 与 idx 0 相同，是同一 PR 的引用。

**Why it matters**: 关联较弱。

### [[InternLM/lmdeploy] TEST: update qwen3.5 397b test (#4607)](https://github.com/InternLM/lmdeploy/commit/0cc6dfb83df42acced885058576a28f08f344d35)
_InternLM/lmdeploy · commit · 2026-05-25 12:29 UTC_

**TL;DR**: 更新了 Qwen3.5 397B 模型的测试用例。

**Why it matters**: 确保大模型推理框架对新型大模型的支持正确性。

### [[InternLM/lmdeploy] #4607 TEST: update qwen3.5 397b test](https://github.com/InternLM/lmdeploy/pull/4607)
_InternLM/lmdeploy · PR · 2026-05-25 12:29 UTC_

**TL;DR**: 与 idx 2 相同，是同一 PR 的引用。

**Why it matters**: 关联较弱。

### [[NVIDIA/Model-Optimizer] #1542 [chore]: weekly bump of uv.lock on main (2026-05-25)](https://github.com/NVIDIA/Model-Optimizer/pull/1542)
_NVIDIA/Model-Optimizer · PR · 2026-05-25 11:14 UTC_

**TL;DR**: 自动更新了 uv.lock 文件中的依赖版本。

**Why it matters**: 关联较弱。

## 研究者 / 博客（1）

### [Can AI write your code?](https://towardsdatascience.com/can-ai-write-your-code/)
_Planet AI · 2026-05-25 17:15 UTC_

**TL;DR**: 一篇关于 ChatGPT 在因果推断中辅助编码的研究文章，评估了 Python、R 和 Stata 上的表现。

**Why it matters**: 关联较弱，属于 AI 辅助编程的通用研究，不直接涉及大模型推理优化。
