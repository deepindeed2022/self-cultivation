# 科研雷达 · 2026-07-05

今日共聚合 1 条动态。

## 今日研究方向建议

1. **阅读今日 Planet AI 条目 "Assemble Each RAG Generation Prompt from a Base Prompt Plus the Rules Each Question Needs" 的 Section 2（提示组装与规则解析），评估其对 vLLM/SGLang 中 prefix-caching 与多轮对话 token 节约方案的参考价值。** 该文提出将固定基础提示与问题规则动态组装，这与多轮对话中需要高效复用上下文缓存、减少重复 token 消耗直接相关。建议你阅读该节，理解其规则解析如何与 KV Cache 的缓存键设计（如如何将规则编码为可复用的前缀）交互。然后，在本地 SGLang 环境中，使用 LLaMA-3-8B 模型，模拟一个 5 轮对话场景，对比启用与未启用该文中的动态提示组装策略在 KV Cache 命中率、首 token 延迟和 token 消耗上的差异，重点关注该策略对 prefix-caching 中缓存键复用效率的实际效果。

2. **分析今日 Planet AI 条目中提示组装策略的调度器设计，评估其对推理系统中 continuous batching 的调度优化参考价值。** 该文提到通过调度器将解析后的问题转化为类型化 LLM 调用，这与 continuous batching 中需要高效调度不同请求的 prefill/decode 阶段高度相似。建议你阅读该文，重点关注其调度器如何根据问题规则分配计算资源。然后，在本地 vLLM 环境中，模拟一个 4 个并发请求的场景（每个请求带有不同规则），对比启用与未启用该文中的调度策略在 batch 延迟、吞吐和请求间公平性上的差异，重点关注该调度器对 continuous batching 中请求优先级管理的启发。

3. **跟进今日动态中提示组装方法对 KV Cache 管理的潜在影响，评估其对 Blackwell 上 FP8/FP4 推理中缓存优化的参考价值。** 该文通过动态组装减少冗余提示，这直接关联 KV Cache 中缓存键的设计（如如何减少重复 token 的缓存占用）。建议你阅读该文，重点关注其规则复用如何与低精度推理中的缓存压缩交互。然后，在本地 TensorRT-LLM 环境中，使用 BERT 模型，模拟在 Blackwell 架构（如 FP8 支持）上，对比启用与未启用该文中的提示组装策略在 KV Cache 显存占用、首 token 延迟和端到端吞吐上的差异，重点关注该策略对低精度推理中缓存效率的实际效果。

## 研究者 / 博客（1）

### [Assemble Each RAG Generation Prompt from a Base Prompt Plus the Rules Each Question Needs](https://towardsdatascience.com/assemble-each-rag-generation-prompt-from-a-base-prompt-plus-the-rules-each-question-needs/)
_Planet AI · 2026-07-05 15:00 UTC_

**TL;DR**: 提出一种RAG生成提示的组装方法：将固定基础提示与每个问题所需的规则结合，通过调度器将解析后的问题转化为类型化的LLM调用。

**Why it matters**: 该方法优化了RAG系统的提示构建流程，提升生成质量与灵活性，与大模型推理中的检索增强生成相关。
